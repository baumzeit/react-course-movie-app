import { SearchResult } from './results-card';

type SearchResultsProps = {
  results: SearchResult[];
};
export const SearchResults = (props: SearchResultsProps) => {
  return <div>Search Results</div>;
};
